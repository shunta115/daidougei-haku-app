# App mode: demo | public
# Unset = demo on Vite DEV server, public on production build
VITE_APP_MODE=public

# Do not put Stripe secrets, Supabase keys, or other private credentials here.
# This project does not require server-side secrets for the public beta SPA.
